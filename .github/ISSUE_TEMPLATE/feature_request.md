---
name: Feature request
about: Suggest a capability (new source, backend, context provider, metric)
labels: enhancement
---

**Problem / use case**
What are you trying to do that OMDG doesn't support today?

**Proposed solution**
If it's a new source / LLM backend / context provider, note which interface it would
implement (`MetadataSource`, `LLMBackend`, `ContextProvider`).

**Alternatives considered**

**Additional context**
